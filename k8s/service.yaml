apiVersion: v1
kind: Service
metadata:
  name: salescast-api
  labels:
    app: salescast
spec:
  type: ClusterIP
  selector:
    app: salescast
  ports:
    - name: http
      port: 80
      targetPort: http
